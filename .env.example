# ── Backend ─────────────────────────────────────────────
OPENAI_API_KEY=xyz
OPENAI_MODEL=gpt-4o-mini
GITHUB_TOKEN=                          # optional, raises rate limit
HELM_CACHE_DIR=/var/cache/helm

# ── Database ───────────────────────────────────────────
POSTGRES_USER=chartsmith
POSTGRES_PASSWORD=changeme
POSTGRES_DB=chartsmith
DATABASE_URL=postgresql+asyncpg://chartsmith:changeme@postgres:5432/chartsmith

# ── Frontend ───────────────────────────────────────────
NEXT_PUBLIC_API_URL=http://localhost:8000
